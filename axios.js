// En esta carpeta se indica la logica para hacer lo mismo

// que en lugar.js, pero utilizando la libreria OPENWEATHER.ORG

const axios = require("axios");

// Con OpenWeather y Axios es mucho mas facil, ya que puedo enviar todo por URL en una peticion.

module.exports = {
  getClima,
};
